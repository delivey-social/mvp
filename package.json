{
  "name": "mvp",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "frontend",
    "backend",
    "dashboard",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\" \"npm run dev --workspace=dashboard\"",
    "build": "concurrently \"npm run build --workspace=frontend\" \"npm run build --workspace=backend\" \"npm run build --workspace=dashboard\"",
    "prepare": "husky",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "husky": "^9.1.7",
    "typescript": "^5.9.3",
    "vite-tsconfig-paths": "^6.1.1"
  },
  "dependencies": {
    "@react-email/render": "^1.0.5",
    "clsx": "^2.1.1",
    "react-email": "^4.0.2"
  }
}
