info:
  name: Update
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{api_url}}/neighborhood/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
