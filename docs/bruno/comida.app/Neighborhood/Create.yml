info:
  name: Create
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_url}}/neighborhood"
  body:
    type: json
    data: |-
      {
        "name": "Teste",
        "base_price": 100
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
