info:
  name: Create
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_url}}/restaurant"
  body:
    type: json
    data: |-
      {
        "name": "Test Created",
        "address": "Test Address 123",
        "CNPJ": "00000000000000"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
