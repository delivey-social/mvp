info:
  name: Ready For Delivery
  type: http
  seq: 6

http:
  method: GET
  url: "{{api_url}}/order/:id/ready-for-delivery"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
