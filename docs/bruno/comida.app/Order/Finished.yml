info:
  name: Finished
  type: http
  seq: 7

http:
  method: GET
  url: "{{api_url}}/order/:id/finished"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
